server:
  port: 8083

spring:
  application:
    name: adoption-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/orphanage?currentSchema=adoption}
    username: ${SPRING_DATASOURCE_USERNAME:orphanage_user}
    password: ${SPRING_DATASOURCE_PASSWORD:orphanage_secret}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: adoption
  flyway:
    enabled: true
    schemas: adoption
    locations: classpath:db/migration
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: 5672
    username: ${SPRING_RABBITMQ_USERNAME:rabbit}
    password: ${SPRING_RABBITMQ_PASSWORD:rabbit_secret}
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://keycloak:8080/realms/orphanage/protocol/openid-connect/certs}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
